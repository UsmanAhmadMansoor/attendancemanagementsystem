<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="usman.attendancemanagementsystem">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".SignUpForm" />
        <activity android:name=".LoginForm" />
        <activity android:name=".Menu" />
        <activity android:name=".ViewAttendance">
        <!--<intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>-->
        </activity>
        <activity android:name=".choosePanel">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AdminSignUpForm" />
        <activity android:name=".AdminLoginForm" />
        <activity android:name=".AdminMenu" />
        <activity android:name=".AddAttendance"></activity>
        <activity android:name=".ViewRequest"></activity>

    </application>

</manifest>

